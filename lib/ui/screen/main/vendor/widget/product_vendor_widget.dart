import 'package:flutter/material.dart';

class ProductVendorWidget extends StatelessWidget {
  const ProductVendorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Product'),
    ));
  }
}
