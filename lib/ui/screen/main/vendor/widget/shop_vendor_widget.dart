import 'package:flutter/material.dart';

class ShopVendorWidget extends StatelessWidget {
  const ShopVendorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Shop'),
    ));
  }
}
