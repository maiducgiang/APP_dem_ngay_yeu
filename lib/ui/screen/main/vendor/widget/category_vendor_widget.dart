import 'package:flutter/material.dart';

class CategoryVendorWidget extends StatelessWidget {
  const CategoryVendorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Category'),
    ));
  }
}
